enum AccountSettingsType {
  changeUserName,
  changeMail,
  changePassword,
  deleteAccount
}

enum NotificationPreferenceEnum { pauseAll, likes, comments, follow, messages }

enum ChatSettingsType {
  chatbg,
  clearChat,
  blocked,
  showLastSeen
}
